export function getTitle(state) {
  return state.medicine.data.data.title;
};

export function getDescription(state) {
  return state.medicine.data.data.description;
}

export function getImage(state) {
  return state.medicine.data.data.description;
}
